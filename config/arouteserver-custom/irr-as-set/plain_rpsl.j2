{#
This is the template used to build the RPSL AS-SET
object of the route server to update the relevant
IRR DBs.

See instructions at the following URL on how to
edit and use it.

https://arouteserver.readthedocs.io/en/latest/USAGE.html#irr-as-set
#}
as-set:   AS-EVIX
descr:    ASNs and AS-SETs for the members of EVIX
remarks:  List of ASes and AS-SETs announced by EVIX
remarks:  clients to the EVIX route servers.
mnt-by:   MAINT-EVIX
{% for member in as_sets_rpsl_objects|sort %}
{% set list2 = member.split('::') %}
members:  {{ list2[-1] }}
{% endfor %}
{% set list1 = cfg.custom_communities.this_date.lrg.split(':') %}
changed:  root@evix.org {{ list1[-1] }}
source:   ALTDB

